sudo: required
services:
  - docker

before_install:
  - docker build -t -f beakerisgod/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI=true beakerisgod/docker-react npm run test -- --coverage  
